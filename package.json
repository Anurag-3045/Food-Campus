{
  "name": "food-del-monorepo-runner",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c yellow,cyan \"npm run server --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "axios": "^1.13.4",
    "react-router-dom": "^7.13.0",
    "react-toastify": "^11.0.5"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
